import React, { Component } from 'react';

export class resume extends Component {
    render() {
        return (
            <div>
                Resume
            </div>
        );
    }
}

export default resume;
